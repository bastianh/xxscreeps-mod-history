---
"xxscreeps-mod-history": minor
---

Advertise `historyKeepTicks` in `/api/version`'s `serverData` alongside
`historyChunkSize`, so the replay viewer knows how far back history reaches
(`0` = kept forever).
